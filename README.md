# Tempo-iOS

SwiftUI app that exports HealthKit data to [Tempo](https://github.com/raulnor/tempo).

## Setup
1. Open in Xcode
2. Update the server URL in `TempoApp.swift`
3. Run on a physical device (HealthKit requires real hardware)
